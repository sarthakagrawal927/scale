# Scale

Standalone local-first systems engineering game. Read PRODUCT.md and DESIGN.md.
Use Node 24 and pnpm. `pnpm test`, `pnpm typecheck`, `pnpm build`.
Do not commit, push, deploy, or create provider resources without explicit authorization.
Keep vendor engine provenance/license. Put game rules in src/game, not vendor simulation. Metrics must come from the engine. Cost and time are explicitly fictional game balancing.
